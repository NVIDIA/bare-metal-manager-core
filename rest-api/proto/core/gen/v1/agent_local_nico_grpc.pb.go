// SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
// SPDX-License-Identifier: Apache-2.0

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.1
// - protoc             (unknown)
// source: agent_local_nico.proto

package core

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	AgentLocal_GetNodeToken_FullMethodName = "/agent_local.AgentLocal/GetNodeToken"
)

// AgentLocalClient is the client API for AgentLocal service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// Local (on-DPU) API served by nico-dpu-agent over a unix domain socket
// (the agent's `local-api-socket` setting, which defaults to a dedicated run/
// subdirectory of the shared credentials directory, so consumers can mount the
// socket read-write while the credentials stay read-only). The literal default
// lives in DEFAULT_AGENT_LOCAL_SOCKET and is deliberately not repeated here:
// REST proto normalization rewrites vendor names inside comments, which would
// silently corrupt an inlined filesystem path into one that does not exist.
// This is the consolidation point for
// agent <-> co-located-service communication: services that today mount the
// machine cert/key (or synced files) to talk to nico-api can instead ask the
// agent, and future local needs should add RPCs here rather than new
// sockets, ports, or file drops.
type AgentLocalClient interface {
	// Returns the current node-auth bearer JWT, minted by the agent from the
	// machine's client-certificate key (issue #355). Callers present it as
	// `Authorization: Bearer <token>` to nico-api and must fetch a fresh one
	// before `expires_at`. Only the agent ever touches the private key.
	GetNodeToken(ctx context.Context, in *GetNodeTokenRequest, opts ...grpc.CallOption) (*GetNodeTokenResponse, error)
}

type agentLocalClient struct {
	cc grpc.ClientConnInterface
}

func NewAgentLocalClient(cc grpc.ClientConnInterface) AgentLocalClient {
	return &agentLocalClient{cc}
}

func (c *agentLocalClient) GetNodeToken(ctx context.Context, in *GetNodeTokenRequest, opts ...grpc.CallOption) (*GetNodeTokenResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(GetNodeTokenResponse)
	err := c.cc.Invoke(ctx, AgentLocal_GetNodeToken_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// AgentLocalServer is the server API for AgentLocal service.
// All implementations should embed UnimplementedAgentLocalServer
// for forward compatibility.
//
// Local (on-DPU) API served by nico-dpu-agent over a unix domain socket
// (the agent's `local-api-socket` setting, which defaults to a dedicated run/
// subdirectory of the shared credentials directory, so consumers can mount the
// socket read-write while the credentials stay read-only). The literal default
// lives in DEFAULT_AGENT_LOCAL_SOCKET and is deliberately not repeated here:
// REST proto normalization rewrites vendor names inside comments, which would
// silently corrupt an inlined filesystem path into one that does not exist.
// This is the consolidation point for
// agent <-> co-located-service communication: services that today mount the
// machine cert/key (or synced files) to talk to nico-api can instead ask the
// agent, and future local needs should add RPCs here rather than new
// sockets, ports, or file drops.
type AgentLocalServer interface {
	// Returns the current node-auth bearer JWT, minted by the agent from the
	// machine's client-certificate key (issue #355). Callers present it as
	// `Authorization: Bearer <token>` to nico-api and must fetch a fresh one
	// before `expires_at`. Only the agent ever touches the private key.
	GetNodeToken(context.Context, *GetNodeTokenRequest) (*GetNodeTokenResponse, error)
}

// UnimplementedAgentLocalServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedAgentLocalServer struct{}

func (UnimplementedAgentLocalServer) GetNodeToken(context.Context, *GetNodeTokenRequest) (*GetNodeTokenResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method GetNodeToken not implemented")
}
func (UnimplementedAgentLocalServer) testEmbeddedByValue() {}

// UnsafeAgentLocalServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to AgentLocalServer will
// result in compilation errors.
type UnsafeAgentLocalServer interface {
	mustEmbedUnimplementedAgentLocalServer()
}

func RegisterAgentLocalServer(s grpc.ServiceRegistrar, srv AgentLocalServer) {
	// If the following call panics, it indicates UnimplementedAgentLocalServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&AgentLocal_ServiceDesc, srv)
}

func _AgentLocal_GetNodeToken_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(GetNodeTokenRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AgentLocalServer).GetNodeToken(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AgentLocal_GetNodeToken_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AgentLocalServer).GetNodeToken(ctx, req.(*GetNodeTokenRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// AgentLocal_ServiceDesc is the grpc.ServiceDesc for AgentLocal service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var AgentLocal_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "agent_local.AgentLocal",
	HandlerType: (*AgentLocalServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "GetNodeToken",
			Handler:    _AgentLocal_GetNodeToken_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "agent_local_nico.proto",
}
