Thank you for installing NVIDIA Forge API version {{ .Chart.AppVersion }}! 

The Forge GRPC endpoint can be reached at: 
{{ ternary "https" "http" (.Values.useTLS) }}://carbide-api.{{ include "common.names.namespace" . }}.svc.{{ .Values.clusterDomain }}:{{ .Values.service.ports.grpc }}

The Postgres database URI is "postgres://<USER>:<PASSWORD>@{{ .Values.postgresClusterName }}-pooler.{{ include "common.names.namespace" . }}.svc.{{ .Values.clusterDomain }}:5432/forge_{{ .Values.databasename }}"

Your release is named {{ .Release.Name }}. To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get manifest {{ .Release.Name }}


