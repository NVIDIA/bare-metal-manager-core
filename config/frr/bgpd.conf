!
router bgp ###REPLACE_ME_WITH_ASN###
 no bgp ebgp-requires-policy
 neighbor FABRIC peer-group
 neighbor FABRIC remote-as internal
 neighbor FABRIC route-map DENY in
 neighbor FABRIC ebgp-multihop
 neighbor ###REPLACE_ME_WITH_PEER### peer-group FABRIC
 !
 address-family ipv4 unicast
  redistribute kernel route-map LEAK
 exit-address-family
 !
route-map LEAK permit 1
  match interface lo
!
route-map LEAK deny 100
!
route-map DENY deny 1
!