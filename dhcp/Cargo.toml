[package]
name = "dhcp"
version = "0.0.1"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
libc = "*"
rpc = { path = "../rpc" }
uuid = "0.8.2"
tokio = { version = "*", features = ["rt"] }
tonic = "0.7.0"
once_cell = "*"
mac_address = "*"
ipnetwork = "*"
log = "*"
derive_builder = "*"

[build-dependencies]
cc = "1.0"
cbindgen = "*"
