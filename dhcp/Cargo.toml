[package]
name = "dhcp"
version = "0.0.1"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
derive_builder = "*"
ipnetwork = "*"
libc = "*"
log = "*"
mac_address = "*"
once_cell = "*"
rpc = { path = "../rpc" }
tokio = { version = "*", features = ["rt"] }
tonic = "0.7.0"
uuid = "0.8.2"

[build-dependencies]
cbindgen = "*"
cc = "1.0"
