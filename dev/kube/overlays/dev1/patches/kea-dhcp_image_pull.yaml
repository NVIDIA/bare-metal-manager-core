apiVersion: apps/v1
kind: Deployment
metadata:
  name: kea-dhcp-deployment
spec:
  template:
    spec:
      containers:
        - name: kea-dhcp
          securityContext:
            capabilities:
              add: ['NET_ADMIN']
          imagePullPolicy: Always
      imagePullSecrets:
        - name: gitlab-container-registry
