apiVersion: batch/v1
kind: Job
metadata:
  name: carbide-db-migrations
spec:
  template:
    spec:
      containers:
      - name: carbide-db-migrations
        envFrom:
        - configMapRef:
            name: carbide-api-config
