{{- with .Values.supplementalNetworkConfig }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nico-dpu-agent.fullname" $ }}-supplemental-network-config
  labels:
    {{- include "nico-dpu-agent.labels" $ | nindent 4 }}
data:
  supplemental.json: |
    {{- . | nindent 4 }}
{{- end }}
